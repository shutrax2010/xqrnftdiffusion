<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="../assets/PhygitalifyQR icon.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <title>Menu</title>
    <style>
        body {
            font: 18px "Open Sans", Helvetica, Arial, sans-serif;
            color: white;
            background-color: #030340;
            margin: 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            margin-bottom: 20px;
        }

        .phygitalify{
            color :#87cefa;
            font-size: 24px;
        }

        .content-container {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            flex-wrap: nowrap;
            width: 50%;
            margin-top: 10%;
        }

        .image-container {
            margin-right: 20px;
        }

        .image-container img {
            max-width: 150px;
            max-height: 150px;
            display: block;
        }

        ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        li {
            margin-bottom: 10px;
        }

        button {
            background-color: transparent;
            padding: 10px 80px;
            margin-bottom: 30px;
            border-radius: 6px;
            color: white;
            cursor: pointer;
            display: block;
            width: 100%;
            text-align: center;
            box-sizing: border-box;
        }
        .social-icons {
            display: flex;
            justify-content: space-evenly;
            margin-top: 20px;
        }

        .social-icons i {
            width: 40px;
            height: 40px;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <h1>Menu</h1>
    <div class="content-container">
        <div class="image-container">
            <img src="../assets/PhygitalifyQR icon.png" style="max-width: 150px;max-height: 150px;" alt="PhygitalifyQR">
            <p class ="phygitalify">PhygitalifyQR</p>
        </div>

        <div>
            <ul>
                <% menuItems.forEach(item=> { %>
                    <li>
                        <button onclick="<%= item.action %>">
                            <%= item.label %>
                        </button>
                    </li>
                    <% }); %>
            </ul>
            <div class="social-icons">
                <i class="fab fa-facebook fa-2x" onclick="navigate('')"></i>
                <i class="fab fa-instagram fa-2x" onclick="navigate('')"></i>
                <i class="fab fa-twitter fa-2x" onclick="navigate('')"></i>
            </div>
        </div>
    </div>
    <script>
        function navigate(url) {
            window.location.href = url;
        }
    </script>
</body>

</html>